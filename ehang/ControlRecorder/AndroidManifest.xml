<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ehang.controlrecorder"
    android:versionCode="1"
    android:versionName="1.0" 
	android:sharedUserId="android.uid.system">
    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="20" />
    <!-- 权限更新的权限 -->
    <uses-permission android:name="android.permission.UPDATE_APP_OPS_STATS" />
    <!-- 电话状态的相应权限 -->
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 开机自启的广播 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
		android:allowClearUserData="false" 
        android:icon="@drawable/subject"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="com.security.controlrecorder.Main"
            android:label="@string/app_name" >
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name="com.ehang.show.ShowActivity"
            android:label="@string/title_activity_show" 
			android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
       <!--注册相应的广播接收器 ，优先级设置成最高值
        <receiver android:name="com.security.controlrecorder.PhoneBroadcastReceiver" >
            <intent-filter android:priority="1000" >
                <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>  --> 
		<!--
        <activity
            android:name="com.ehang.controlrecorder.WelcomeActivity"
            android:label="@string/title_activity_show" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>  -->
        
        <!-- 注册一个开机自启的广播 -->
        <!-- 当应用程序启动时它是第一个显示的activity -->
        <receiver android:name="com.ehang.broacastreceiver.kaiji">
           <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
            
        </receiver>
        <receiver android:name="com.ehang.broacastreceiver.appUpdate">
            <intent-filter android:priority="1000">
             <action android:name="android.intent.action.PACKAGE_ADDED" />  
             <action android:name="android.intent.action.PACKAGE_REMOVED" />  
            <data android:scheme="package" />  
            </intent-filter>
        </receiver>
        <provider android:name="com.ehang.contentprovider.switchContentprovider"
            android:authorities="com.ehang.contentprovider.switchContentprovider"
            android:exported="true">
            
        </provider>
        
    </application>
</manifest>